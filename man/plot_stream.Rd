% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_stream.R
\name{plot_stream}
\alias{plot_stream}
\title{Stream Plot of Parental Influence Over Time for a Target Node}
\usage{
plot_stream(mdm_object, child_node, distribution = "filt")
}
\arguments{
\item{mdm_object}{An object of class \code{"mdm"} as returned by \code{\link{mdm}}.}

\item{child_node}{Integer index of the target node (i.e., the child whose parents' effects are shown).}

\item{distribution}{Whether to use filtered ("filt") or smoothed ("smoo") posterior estimates.
Default is \code{"filt"}.}
}
\value{
A \code{ggplot} object containing the stream plot of parental influences.
}
\description{
Visualizes the dynamic influence of each parent on a specific child node in a Bayesian dynamic network,
using streamgraph-style visualization based on posterior mean values.
}
\details{
This function is useful for analyzing how the strength of influence from different parent nodes varies
across time for a specific target node.
}
\examples{
\dontrun{
# Fit model
result <- mdm(data_input = y)

# Plot stream graph of influence on node 2
plot_stream(result, child_node = 2)

}
}
