% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_arcs.R
\name{plot_arcs}
\alias{plot_arcs}
\title{Plot Dynamic Parameters of a Bayesian Network Over Time}
\usage{
plot_arcs(
  mdm_object,
  type = c("connections", "intercepts", "all"),
  distribution = "filt",
  ci_level = 0.95
)
}
\arguments{
\item{mdm_object}{An object of class \code{"mdm"} returned by \code{\link{mdm}}.}

\item{type}{Character string indicating which parameters to plot.
Choices are:
\describe{
  \item{"connections"}{Plots only dynamic edge parameters (e.g., V3->V1).}
  \item{"intercepts"}{Plots only intercepts (e.g., beta0 terms).}
  \item{"all"}{Plots both intercepts and connections.}
}}

\item{distribution}{Whether to use filtered ("filt") or smoothed ("smoo") distributions.
Default is "filt".}

\item{ci_level}{Numeric value between 0 and 1. Width of the confidence/credible interval.
Default is 0.95.}
}
\value{
A \code{ggplot} object showing the posterior trajectories with uncertainty bounds.
}
\description{
This function plots time series of either intercepts, connection strengths,
or both from an object of class \code{"mdm"} using posterior means and
credible/confidence intervals. The user can select between filtered or smoothed estimates.
}
\examples{
\dontrun{
# Fit model
result <- mdm(data_input = y)

# Plot dynamic connectivity (filtered)
plot_arcs(result, type = "connections")

# Plot smoothed intercepts
plot_arcs(result, type = "intercepts", distribution = "smoo")
}
}
